<template>
  <div class="barber-card mb-3 rounded-4 overflow-hidden">
    <div class="row g-0">
      <div class="col-12 col-md-4">
        <img :src="barber.img" class="w-100 h-100 barber-img" alt="barber" />
      </div>
      <div class="col-12 col-md-8">
        <div class="p-3 h-100 d-flex flex-column justify-content-between">
          <div>
            <h5 class="barber-title">{{ barber.name }}</h5>
            <div class="mb-1">
              <span class="text-warning">★ ★ ★ ★ ★</span>
              <small class="text-muted ms-2">{{ barber.rating }}</small>
            </div>
            <p class="mb-1">📍 {{ barber.address }}</p>
            <p class="mb-2">⏱️ {{ barber.status }}</p>
          </div>
          <div>
            <button
              class="btn btn-dark rounded-pill px-4 py-1"
              @click="$emit('read-more', barber)"
            >
              Read more...
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
export default {
  name: "BarberCard",
  props: {
    barber: { type: Object, required: true },
  },
  emits: ["read-more"],
};
</script>

<style scoped>
.barber-card {
  background: #fff;
  box-shadow: 0 6px 14px rgba(0, 0, 0, 0.08);
}
.barber-img {
  object-fit: cover;
  min-height: 150px;
}
.barber-title {
  font-family: Georgia, serif;
}
</style>
